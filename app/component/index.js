import SparklinesDirective from './component.directive';

let app = angular.module('sanji.sparklines', []);
app.directive('sparklines', SparklinesDirective.directiveFactory);
export default app = app.name
